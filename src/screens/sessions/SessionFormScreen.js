import React from 'react';
import LiteracySessionForm from './LiteracySessionForm';

export default function SessionFormScreen({ navigation, route }) {
  return <LiteracySessionForm navigation={navigation} route={route} />;
}
